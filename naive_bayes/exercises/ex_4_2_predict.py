# Exercise 4.2 — predict (N features)
# Write your solution in this file. Run test_exercises.py to check.

from exercises.ex_2_5_probability import probability

def predict(data, features, label_col, labels):
    """
    features: dict mapping feature_name -> feature_value,
              e.g. {"Outlook": "Sunny", "Temperature": "Hot", "Humidity": "High"}
    For each label, compute: P(label) * product of P(feature_value | label) for all features.
    Return the label with the highest score.
    """
    pass
