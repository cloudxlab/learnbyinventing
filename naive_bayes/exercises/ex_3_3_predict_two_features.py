# Exercise 3.3 — predict_two_features
# Write your solution in this file. Run test_exercises.py to check.

from exercises.ex_2_5_probability import probability

def predict_two_features(data, f1_name, f1_val, f2_name, f2_val, label_col, labels):
    """
    For each label, compute:
      score = P(f1_val | label) * P(f2_val | label) * P(label)
    Return the label with the highest score.
    """
    pass
