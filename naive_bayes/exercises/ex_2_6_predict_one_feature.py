# Exercise 2.6 — predict_one_feature
# Write your solution in this file. Run test_exercises.py to check.

from exercises.ex_2_5_probability import probability

def predict_one_feature(data, feature_name, feature_value, label_col, labels):
    """
    For each label, compute:  score = P(feature_value | label) * P(label)
    Return the label with the highest score.
    """
    pass
