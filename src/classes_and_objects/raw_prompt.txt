Classes & Objects chapter — from python_iitr_feb2025 (May3/OOPs.ipynb, May 17/problem.ipynb)
plus the OOP gap noted in python_iitr_apr_24 (jun 22: Customer class, StLine2pt fit/predict,
"repeat for 3D").

Source exercises:
- customer dict -> class Customer with attributes and a method
- class TwoPointLine: fit(x, y) with two points -> m, c stored on self;
  predict(list) -> list. model.fit([2,5],[3,9]); thermometer calibration [1,2]->[30,35].
  Note model.predict(x) == TwoPointLine.predict(model, x). Validation: reject != 2 points.
- inheritance: class Animal (eat), class Dog(Animal) (bark)
- Tree Node with total() and count(); TODOs from source: min, max, find(x),
  max_depth (source version crashes on leaves - fix), translate_update_value(func) -> apply
  Example tree: Node(10, Node(4, Node(1), Node(11)), Node(5, Node(15), Node(16)))
- DecisionNode(criteria, boundary, left, right).answer(circumstance dict):
  < boundary -> left, >= -> right; Yes/No leaf classes inherit DecisionNode.
  Job offer tree: salary -> learningOpp -> dist, tests from source.
- May 17: "Question: Save and Load the decision tree to/from a file." -> to_dict/from_dict
  + json round trip; aside on pickle trade-offs.
- FitPlane (3D line-fit extension): three points determine plane z = ax + by + d;
  build equations [x, y, 1, z], reuse solve_equations from Loops and Arrays.

Pedagogical arc: data+behavior -> the fit/predict model API (sklearn shape) ->
inheritance -> recursion on object trees -> decision tree as objects -> serialization ->
bonus: fit = equation solving. Bridges into Ancient Secrets of Prediction, Gradient
Descent and Decision Trees chapters.
