Square root:
    - Open a Jupyter notebook or Google collab. Use python as a calculator for every step below.
    - You can simple type an expression such as 3*4 into the cell and shift+ENTER
    - what is square of 2? Hint: square means a number multiplied by itself.
    - What is square of 3, 4, 5?
    - What is square root of 16, 25, 9? Hint: Check your answer by squaring your guess using python code.
    - Now, what is square root of 5? Just take a guess? Hint: it should be between 2 and 3?
    - Check what is your guess? Hint: Square your number and check.
    - So, is your guess bigger or smaller? Now, quickly take next guess?
    Repeat these last two steps.
    - your next guess will be between which two numbers? Call them lo and hi and then calculate guess = (lo + hi)/2. Use python to make your like easier.
    - Next, you can automatically change lo = guess or hi = guess depending upon whether your guess squared was lower     than 5 or higher than 5 using python's if-else condition. Hint: [to Claude: show the if-else logic]
    - Now, return the logic again and again. Do you see your guess squared getting closer and closer
    - Next you can run your logic, say 20 times using for loop.
    - Did you guess squared got closer to 5? If yes, congratulations! Your 'guess' is the square root of 5. Check your answer with import math; math.sqrt(5)
    - Amazing!
    - Now, repeat the above process for square root of 10.
    - Wonderful!
    - Now, consolidate your for-loop, if condition into a consolidated python function 'mysqrt'. This function should take num of which you want to calculate sqrt, initial lo and initial hi and it should take how iterations it should run. It should return the guess:
    def mysqrt(num, lo, hi, iterations):
        for I in range(iterations):
            guess = (lo + hi)/2
            if guess*guess > num:
                [Your code goes here]
            else:
                [Your code goes here]
        return guess
    - test it with mysqrt(15, 3, 4, 20) and compare the results with math.sqrt(15)

    - Congratulation you learnt basics of python and binary search algorithms  in single shot!
