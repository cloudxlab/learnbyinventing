Create presentation for teaching a class metrics of classification by the way of learn by inventing.

Here is are two arrays one containing actual and other predicted labels of binary classification:
- Generate example arrays
- Generate confusion matrix and explain each column and row
- Ask user to prepare a confusion matrix for the arrays by hand
- Ask user to create function for generating confusion matrix. Provide the function signature.
- Explain precision, recall, F1 Score, TPR, FPR and significance of each
- Ask user to calculate by hand given the confusion matrix
- Ask user to create functions for each. Provide function signatures.
- Provide the user with decimal numbers as model outputs. Ask the user to write function that converts the output using a threshold.
- [Question is how to decide on this threshold?]
- [for each threshold value calculate precision] [Pick the threshold that gives you the best precision]
- [for each threshold value calculate recall] [Pick the threshold that gives you the best recall]
- [for each threshold value calculate F1 Score] [Pick the threshold that gives you the best F1 Score]
- Guide user to plot Precision, recall, F1 Score vs Threshold
- Provide the user with the code of training logistic regression on iris dataset and preparing the outcome.
  Let the user use the function to create the above plot for this data.
- Guide user to prepare TPR/FPR (ROC) plot
- Ask user to write function that calculates area under it (AUC). Explain the significance of AUC ROC
- Provide the user with the code of training two different models on iris.
  Let the user find the AUC ROC for each and identify the better model.
